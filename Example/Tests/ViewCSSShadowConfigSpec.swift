/*import Quick
import Nimble
import ViewCSS

class ViewCSSShadowConfigSpec: QuickSpec {
    override func spec() {
        describe("#fromCSS") {
            it("ignores everything if the v-shadow and h-shadow are not defined") {
                let css = [
                    "text-shadow" : ""
                ]
                
                let config = ViewCSSShadowConfig.fromCSS(dict: "css", base: "text")
                expect(config.offset) == nil
            }
        }
    }
}*/
